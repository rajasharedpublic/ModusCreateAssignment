# ExpCuke gemfile, ruby 1.9.3
source "http://rubygems.org"

#I understand only gem which selenium Webdriver need to be used,but we need
# One runner(test-unit is required)- 10th Point(Assignment, refer Readme.md file) need to cover with this.
# Even Though watir provides excellent wrapper over selenium, but as per instruction not using it
#
# Install on terminal which this command
# bundle install --path vendor/bundle

gem "selenium-webdriver", "3.4.1"
#gem "selenium-webdriver", "3.14.0"
gem 'test-unit' #10th Point(Assignment, refer Readme.md file) need to cover with this.
gem "ffi", "1.9.18" # If in case some interdependent file gives error