# ExpCuke gemfile, ruby 1.9.3
source "http://rubygems.org"

# I understand only one gem which is selenium Webdriver need to be used,but we need one runner to start the execution
# as well as for assertion given 10th step given in Readme.md file
# One runner(est-unit is required)
# Even Though watir provides excellent wrapper over selenium, but as per instruction not using it here

gem "selenium-webdriver", "3.4.1"
gem 'test-unit'